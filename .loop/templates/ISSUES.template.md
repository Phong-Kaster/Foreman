# LOOP ISSUES REPORT

> Regenerated every iteration and kept at the **repository root**, so it survives the Cleanup Commit
> that removes `.ai/`. This is what a human reads when they come back to the run.
>
> Problems only. What succeeded is in the commit messages.

_Last updated: <timestamp> - branch `loop/...` - iteration N_

## Abandoned tasks

<!-- Each task that failed three attempts. Include all three: what was tried, the command, and the
     error tail. A failed attempt is reverted and enters no commit, so this is the only record. -->

### T-00x - <title>
- **Attempt 1:** <approach> - `<command>` - error: <tail>
- **Attempt 2:** ...
- **Attempt 3:** ...
- **Conclusion:** <what the engine believes is actually wrong>

## Unreachable tasks

<!-- Tasks not attempted because they depend on an abandoned task. -->
| Task | Blocked by |
|---|---|

## Decisions awaiting an answer

<!-- From the Decision Queue. Each names the tasks it blocks. -->
| # | Question | Blocks |
|---|---|---|

## Review findings not fixed

<!-- Major/Minor findings recorded but not acted on. These would otherwise vanish with `.ai/`. -->

## Assumptions recorded

<!-- Minor ambiguities the engine resolved by assumption rather than by asking. Reversible. -->
